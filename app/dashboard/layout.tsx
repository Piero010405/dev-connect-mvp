import type React from "react"
export const metadata = {
  title: "Dashboard - DevConnect",
  description: "Dashboard de DevConnect - Evaluaciones técnicas",
}

export default function DashboardRootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
